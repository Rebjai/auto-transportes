<div class="container p-4">
    <div class="row">
        {{#each ruta}}
        <div class="col-md-3">
            <div class="card text-center">
                <div class="card-body">
                    {{!-- <a href="{{url}}" target="_blank"> --}}
                        <h3 class="card-title text-uppercase">
                            {{origen}} - {{destino}}
                        </h3>
                    {{!-- </a> --}}
                    <p class="m-2">{{hora}}</p>
                    <p class="text-muted">
                        {{vehiculo}}
                    </p>
                    <a class="btn btn-danger" href="/rutas/delete/{{id}}">
                        Delete Link
                    </a>
                    <a class="btn btn-secondary" href="/rutas/edit/{{id}}">
                        Edit
                    </a>
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>Aún no hay rutas creadas.</p>
                <a href="/rutas/add">Crear una!</a>
            </div>
        </div>
        {{/each}}
    </div>
</div>